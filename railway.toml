[build]
builder = "nixpacks"
buildCommand = "cd backend && ./gradlew bootJar -x test"

[deploy]
startCommand = "java -jar backend/build/libs/backend-0.0.1-SNAPSHOT.jar"
restartPolicyType = "on_failure"
