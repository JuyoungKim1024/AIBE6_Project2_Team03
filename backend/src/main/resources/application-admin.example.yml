# Copy this block into application-local.yml.
# Share the actual values through a private team channel. Never commit real credentials.
app:
  admin:
    email: YOUR_ADMIN_EMAIL
    password: YOUR_ADMIN_PASSWORD
