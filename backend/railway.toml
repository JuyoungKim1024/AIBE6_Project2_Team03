[build]
builder = "RAILPACK"
buildCommand = "./gradlew bootJar -x test"

[deploy]
startCommand = "java -Duser.timezone=Asia/Seoul -jar build/libs/backend-0.0.1-SNAPSHOT.jar"
restartPolicyType = "on_failure"
